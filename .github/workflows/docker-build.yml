name: 🚀 Build and Push Docker Image with Supervisor Runtime

on:
  push:
    branches:
      - main

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: chaos4455/message-broker-replika
      VERSION: latest
      DOCKERHUB_USERNAME: chaos4455

    steps:
      - name: 🧱 Checkout Repository
        uses: actions/checkout@v3

      - name: 🧾 Create Dockerfile dynamically
        run: |
          cat <<'EOF' > Dockerfile
          FROM ubuntu:22.04
          ENV DEBIAN_FRONTEND=noninteractive

          RUN apt-get update && \
              apt-get upgrade -y && \
              apt-get install -y python3 python3-pip curl git net-tools iputils-ping nano openssh-server supervisor && \
              pip3 install --no-cache-dir \
                fastapi \
                uvicorn[standard] \
                tortoise-orm \
                aiosqlite \
                pydantic[email] \
                python-jose[cryptography] \
                colorama \
                cryptography \
                psutil \
                Werkzeug \
                slowapi \
                strawberry-graphql[fastapi] \
                Jinja2 \
                ipaddress \
                passlib \
                requests \
                schedule \
                flask \
                flask_cors

          RUN useradd -m -s /bin/bash replika && \
              echo 'replika:replika' | chpasswd && \
              mkdir -p /home/replika/app /var/log/supervisor

          WORKDIR /home/replika/app
          COPY . .

          COPY supervisord.conf /etc/supervisor/supervisord.conf

          CMD ["/usr/bin/supervisord", "-n", "-c", "/etc/supervisor/supervisord.conf"]
          EOF

      - name: ⚙️ Create supervisord.conf dynamically
        run: |
          cat <<'EOF' > supervisord.conf
          [supervisord]
          nodaemon=true
          logfile=/var/log/supervisor/supervisord.log
          pidfile=/tmp/supervisord.pid

          [program:broker]
          command=python3 /home/replika/app/message-broker-v3-clean.py
          directory=/home/replika/app
          autostart=true
          autorestart=true
          startsecs=3
          stderr_logfile=/var/log/supervisor/broker.err.log
          stdout_logfile=/var/log/supervisor/broker.out.log

          [program:webdash]
          command=python3 /home/replika/app/webdashv2-clean.py
          directory=/home/replika/app
          autostart=true
          autorestart=true
          startsecs=3
          stderr_logfile=/var/log/supervisor/webdash.err.log
          stdout_logfile=/var/log/supervisor/webdash.out.log
          EOF

      - name: 🐳 Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🛠️ Build Docker Image
        run: |
          docker build -t $IMAGE_NAME:$VERSION .

      - name: 📤 Push Docker Image
        run: |
          docker push $IMAGE_NAME:$VERSION

      - name: 🚀 Deploy & Test Ports
        run: |
          docker run -d --name replika_test_container -p 8333:8333 -p 8777:8777 $IMAGE_NAME:$VERSION
          echo '⏳ Aguardando 15s para inicialização dos serviços...'
          sleep 15
          echo '🔍 Testando se as portas estão respondendo...'
          nc -zv localhost 8333 || echo '⚠️ Porta 8333 não está respondendo.'
          nc -zv localhost 8777 || echo '⚠️ Porta 8777 não está respondendo.'
          echo '✅ Teste de portas finalizado.'
          echo '📜 Últimos logs do supervisord:'
          docker exec replika_test_container tail -n 50 /var/log/supervisor/supervisord.log || echo '⚠️ Não foi possível obter logs.'
          echo '📜 Últimos logs do broker:'
          docker exec replika_test_container tail -n 50 /var/log/supervisor/broker.out.log || echo '⚠️ Sem logs.'
          echo '📜 Últimos logs do dashboard:'
          docker exec replika_test_container tail -n 50 /var/log/supervisor/webdash.out.log || echo '⚠️ Sem logs.'

      # - name: 🏷️ Optional: Create and push tag
      #   run: |
      #     git config --global user.name "Elias Andrade - Replika AI"
      #     git config --global user.email "chaos4455@users.noreply.github.com"
      #     git tag RC1-beta-v0001
      #     git push origin --tags
